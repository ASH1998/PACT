# PACT Backend Configuration

# Database
DATABASE_URL=sqlite+aiosqlite:///./pact.db

# Server
HOST=0.0.0.0
PORT=8000

# LLM (for agent runtime, optional)
LLM_PROVIDER=openai
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

# Crypto
PASSPORT_ISSUER_KEY_PATH=./keys/issuer.key
